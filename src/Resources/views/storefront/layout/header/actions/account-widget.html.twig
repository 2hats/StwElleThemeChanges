{% sw_extends '@Storefront/storefront/layout/header/actions/account-widget.html.twig' %}

{% block layout_header_actions_account_widget %}
	{% if config('StwElleThemeChanges.config.enable') %}
<div class="dropdown">
		{% block layout_header_actions_account_widget_dropdown_button %}
		{% if config('StwElleThemeChanges.config.enable') %}
       <button class="btn account-menu-btn header-actions-btn" type="button" id="accountWidget" data-offcanvas-account-menu="true"  data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" aria-label="{{ "account.myAccount"|trans|striptags }}" title="{{ "account.myAccount"|trans|striptags }}">
				{% sw_icon 'service' style {
											'namespace': 'StwElleThemeChanges',
											'pack': 'solid'
                                        } %}
			</button>
        {% else %}
		{{ parent() }}
	{% endif %}
			
		{% endblock %}

		{% block layout_header_actions_account_widget_dropdown_menu %}
			{{ parent() }}
		{% endblock %}
	</div>
	{% else %}
		{{ parent() }}
	{% endif %}
	
{% endblock %}
